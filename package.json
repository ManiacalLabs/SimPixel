{
  "name": "SimPixel",
  "version": "1.0.2",
  "description": "A WebGL LED visualizer.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --serveStatic src --files 'src/**/*' --no-inject-changes --directory",
    "build": "babel src --out-dir dist --copy-files --presets=env"
  },
  "author": "mwcz <mwc@clayto.com> (https://palebluepixel.org)",
  "license": "GPL-3.0",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.6",
    "browser-sync": "^2.18.2"
  }
}
