// A complete configuration panel could be implemented here.  For now, it's
// just a single button for changing the BiblioPixel host.

function addHostButton(onclick) {
    // const link = document.createElement('a');
    // link.innerHTML = 'Change Host';
    // link.href = '#';
    // link.addEventListener('click', evt => {
    //     evt.preventDefault();
    //     onclick();
    // });
    // link.classList.add('conf');
    // document.body.appendChild(link);
}
